import type { PackageBuildOptions } from '../../packageBuild.config';

const packageBuild = {
  styleDependencies: ['katex/dist/katex.min.css'],
} satisfies PackageBuildOptions;

export default packageBuild;
