---
title: 🧪 博客内容能力 Demo
slug: demo
tags:
  - notes
publishedAt: 2026-04-30
updatedAt: 2026-04-30
summary: 这是一篇集中展示博客正文能力的 demo，后续写文章时可以直接对照这里的写法。
---

这篇文章专门用来放博客正文支持的各种 case。图片统一放在文章自己的 `image/` 文件夹里，正文直接写相对路径就行。

This is an English paragraph for typography testing. The quick brown fox jumps over the lazy dog, and the page should keep the same rhythm when Chinese and Latin characters appear together.

## 1. 基础正文

普通段落、`inline code`、[站外链接](https://example.com) 都应该自然工作。**加粗**字体应该正常显示。

### 1.1 颜色示例

推荐用更短的写法 <Color c="green">`这是一段绿色文字`</Color>，正文里直接用预设颜色名就行。

目前内置了 8 个预设颜色：

- <Color c="gray">gray</Color>
- <Color c="blue">blue</Color>
- <Color c="green">green</Color>
- <Color c="cyan">cyan</Color>
- <Color c="orange">orange</Color>
- <Color c="red">red</Color>
- <Color c="purple">purple</Color>
- <Color c="pink">pink</Color>

如果偶尔需要临时颜色值，依然可以继续写 <Color color="#8b38aa">自定义颜色</Color>，但更推荐优先用预设色。

> 如果内容系统写起来不顺手，页面再漂亮也很难持续更新。

- 一段简短说明
- 一组并列要点
- 一些穿插在正文里的链接和代码片段

---

## 2. 标题层级

二级标题和三级标题都会进入阅读结构里，所以正文里正常写就行。

### 2.1 小标题示例

正文不需要刻意“写得像 demo”，只要让未来的你看一眼就知道这个能力怎么用就够了。

## 3. 列表和代码块

```ts {2-4,7,9-11} ln
type BlogArticleFrontmatter = {
  title: string;
  slug: string;
  tags: Array<string>;
  publishedAt: string;
  updatedAt: string;
  summary?: string;
  cover?: string;
};

const createBlogTagNavigation = (tag?: string) => {
  if (!tag) return { pathname: '/blog' };
  return { pathname: '/blog', search: `?tag=${tag}` };
};
```

## 4. 单张图片

Markdown 图片直接写相对路径就行，不需要 `import`。

![博客正文单图示例](./cover.jpg '这是一张正文里的单图，点击可以放大')

## 5. 数学表达式

行内公式可以直接写在正文里，比如欧拉恒等式 $e^{i\pi} + 1 = 0$，或者二次方程求根公式 $x = \frac{-b \pm \sqrt{b^2 - 4ac}}{2a}$。

块级公式适合推导和单独展示：

$$
\int_{-\infty}^{\infty} e^{-x^2} \, dx = \sqrt{\pi}
$$

多行公式也可以正常排版：

$$
\begin{aligned}
f(x) &= ax^2 + bx + c \\
f'(x) &= 2ax + b
\end{aligned}
$$

## 6. 四宫格示例

<ImageGallery
  columns={2}
  images={[
    { src: './cover.jpg', alt: '四宫格 1' },
    { src: './cover.jpg', alt: '四宫格 2' },
    { src: './cover.jpg', alt: '四宫格 3' },
    { src: './cover.jpg', alt: '四宫格 4' },
  ]}
/>

## 7. 六宫格示例

<ImageGallery
  columns={3}
  images={[
    { src: './cover.jpg', alt: '六宫格 1' },
    { src: './cover.jpg', alt: '六宫格 2' },
    { src: './cover.jpg', alt: '六宫格 3' },
    { src: './cover.jpg', alt: '六宫格 4' },
    { src: './cover.jpg', alt: '六宫格 5' },
    { src: './cover.jpg', alt: '六宫格 6' },
  ]}
/>

## 8. 后续约定

- 单图优先用 Markdown 原生语法
- 多图排版用 `ImageGallery`
- 数学表达式用 `$...$` 写行内公式，用 `$$...$$` 写块级公式
- `columns={2}` 适合四宫格
- `columns={3}` 适合六宫格或九宫格
- `summary` 现在是可选的，不写也能发文
